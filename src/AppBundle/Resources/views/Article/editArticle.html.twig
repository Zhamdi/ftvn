{% extends "AppBundle::layout.html.twig" %}

{% block title "Symfony - Articles" %}

{% block content_header '' %}

{% block content %}
    <h1 class="title">{{ 'article.edit.headline'|trans({'%id%': id}, 'AppBundle') }}</h1>

    <form action="{{ url('put_articles', {'id': id}) }}" method="POST" {{ form_enctype(form) }}>
        <input type="hidden" name="_method" value="put" />

        {{ form_widget(form) }}

        <input type="submit" value="{% trans from 'AppBundle' %}article.edit.submit{% endtrans %}">
    </form>
{% endblock %}

{% set code = code(_self) %}
